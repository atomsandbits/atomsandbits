import React from 'react';

const styles = (theme) => ({
  container: {
    margin: '0 auto 30px',
  },
  speckContainer: {
    textAlign: 'center',
    overflow: 'hidden',
    userSelect: 'none',
    maxHeight: 375,
  },
  speckCanvas: {
    width: '100%',
  },
});

export default styles;
