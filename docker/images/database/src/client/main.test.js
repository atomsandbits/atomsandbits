import 'mocha';
import { expect } from 'chai';

describe('Client Test demo', () => {
  it('test should equal test', () => {
    const testVar = 'test';
    expect(testVar).to.equal('test');
  });
});
